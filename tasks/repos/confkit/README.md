# confkit

Typed application settings resolved from an ordered list of sources (a
dictionary, environment variables). The first source that defines a key wins.

Run the tests with `python -m pytest`.
