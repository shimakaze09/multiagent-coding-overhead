# flowq

Runs dependent jobs on a simulated clock with priorities, retries with
backoff, resource capacity, and crash/resume snapshots.

    python -m pytest tests

Document format: `docs/workflows.md`. Scheduling rules: `docs/scheduling.md`.
