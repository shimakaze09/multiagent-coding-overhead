# shipkit

Parcel shipping quotes for the web shop checkout.

A quote bills the larger of a parcel's actual and volumetric weight against the
carrier's rate card, then applies the destination zone. Weights and dimensions
can be entered in metric or imperial units.

Run the tests with `python -m pytest`.
