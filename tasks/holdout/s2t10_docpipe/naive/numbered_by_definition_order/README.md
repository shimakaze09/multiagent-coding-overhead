# docpipe

Renders a Markdown subset to HTML or plain text: headings with ids, lists,
quotes, code blocks, inline markup, links, footnotes, a table of contents, an
anchor checker and a render cache.

    python -m pytest tests

Syntax: `docs/syntax.md`. Extending the renderers: `docs/extending.md`.
Untrusted input: `docs/security.md`.
