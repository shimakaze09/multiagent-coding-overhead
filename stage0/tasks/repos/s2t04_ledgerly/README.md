# ledgerly

A small double-entry bookkeeping library: money and currencies, exchange
rates, journal entries, a ledger with accounting periods, reports, bank and
rate-file import, JSON persistence and invoice revenue splits.

    python -m pytest tests

Project conventions (rounding, allocation, rates, file compatibility) are in
`docs/conventions.md`.
